<svg xmlns="http://www.w3.org/2000/svg" width="650" height="100" viewBox="0 0 650 100" role="img" aria-label="Hi Kasper here">
  <rect width="100%" height="100%" fill="transparent" />

  <text x="20" y="65" font-family="monospace" font-size="48" letter-spacing="2" fill="black">
    ✨ Hi
    <tspan id="scramble-header">Kasper</tspan>
    here ✨
  </text>

  <script type="application/ecmascript"><![CDATA[
    const phrases = ["Kasper", "Laban"];
    const el = document.getElementById("scramble-header");

    const randomChars =
      "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789" +
      "αβγδεζηθλμνξπστυφχψω" +
      "あいうえおかきくけこさしすせそ" +
      "アイウエオカキクケコサシスセソ";

    function randomChar() {
      return randomChars[Math.floor(Math.random() * randomChars.length)];
    }

    async function scrambleTo(text) {
      const oldText = el.textContent || "";
      const maxLen = Math.max(oldText.length, text.length);

      for (let i = 0; i <= maxLen; i++) {
        let output = "";

        for (let j = 0; j < maxLen; j++) {
          if (j < i) {
            output += text[j] || "";
          } else {
            output += randomChar();
          }
        }

        el.textContent = output;
        await new Promise((r) => setTimeout(r, 60));
      }

      el.textContent = text;
    }

    async function loop() {
      let i = 0;

      while (true) {
        await scrambleTo(phrases[i]);
        await new Promise((r) => setTimeout(r, 2000));
        i = (i + 1) % phrases.length;
      }
    }

    loop();
  ]]></script>
</svg>
